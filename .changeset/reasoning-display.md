---
"@glrs-dev/glrs": minor
---

Keep provider-supplied reasoning visible in transcripts and print mode by default. Add `reasoningDisplay` config to hide it or require a minimum configured reasoning effort.
