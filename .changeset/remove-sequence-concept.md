---
"@glrs-dev/glrs": minor
---

Remove the sequence workflow, `$name` shortcuts, sequence discovery, and sequence references from the runtime and documentation. Reusable behavior belongs in commands or extensions.
