---
"@glrs-dev/glrs": minor
---

Initialize missing User config on the first CLI run and, inside a Git repository, initialize Project and Project-User config with the hosted JSON Schema link. Existing files are never overwritten.
