---
"@glrs-dev/glrs": minor
---

Expand the published documentation with shared user-facing references, configuration guidance, troubleshooting, and a generated extension API reference. Add configurable built-in tool timeouts through `tool_timeout_ms` and `GLORIOUS_TOOL_TIMEOUT_MS`.
