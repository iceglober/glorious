---
"@glrs-dev/glrs": major
---

Require every run to configure a fully qualified `provider/model-id` through CLI, environment, or config. Remove the implicit Azure provider and default model fallbacks.

Rename shipped-extension terminology to first-party extensions and publish richer TSDoc for the Extension API and SDK.
