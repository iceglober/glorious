{
  "goal": "Add a slugify(value) function to src/text.ts that lowercases and hyphenates, with a test in src/text.test.ts, then run bun test",
  "check": "bun test 2>&1 | grep -q '0 fail' && grep -q 'slugify' src/text.ts && grep -q 'slugify' src/text.test.ts"
}
