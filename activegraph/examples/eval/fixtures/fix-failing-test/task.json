{
  "goal": "The test suite is failing. Find out why and fix the source so bun test passes. Do not change the test.",
  "check": "bun test 2>&1 | grep -q '0 fail' && diff -q src/math.test.ts .expected-math-test.txt && test $(bun test 2>&1 | grep -oE '[0-9]+ pass' | grep -oE '[0-9]+') -ge 2"
}
