# Node / TS
node_modules/
dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Env
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo

# Bun
.bun/

# coder runtime state written into target repos
.coder/proposals/
.coder/fixtures/
.coder/*.jsonl

.lab/
run.log

# eval harness run artifacts (traj dumps + result rows)
core/eval/results/
core/eval/traj/

# glorious's own runtime config while dogfooding itself — transient per-user
# model/scope selections, not a shared project default. Skills/extensions under
# .glorious/ stay tracked; only the config files are ignored.
/.glorious/config.json
/.glorious/config.local.json

# local agent event log
coding-agent.db*
