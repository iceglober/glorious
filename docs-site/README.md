# glrs.dev

The documentation site is generated by [TypeDoc](https://typedoc.org/).

- `/` is a project landing page. `projectLanding` in `typedoc.json` controls the project label and URL prefix; `projectDocuments` controls its guides.
- API reference documents only two intentional public entry points: Extension API and SDK.
- Long-form guides use TypeDoc External Documents from `docs/published/**/*.md`.
- `theme/index.ts` registers the custom `glrs` theme.
- `theme/glrs.css` carries the glrs.dev visual system.
- `@boneskull/typedoc-plugin-mermaid` renders Mermaid diagrams with a local pinned Mermaid bundle.
- `plugins/project-landing.ts` builds the root project link and prefixes every generated project page.
- `plugins/group-documents.ts` nests globbed External Documents under non-navigable repository-directory headings and keeps all documents in path order before generated API pages. Set `lowercaseDocumentGroupTitles` in `typedoc.json` to lowercase those headings.

```sh
cd docs-site
bun install
bun run build
bun run dev
```

The local server runs at `http://127.0.0.1:4180/`. `bun run dev` watches project indexes, External Documents, API entry points, TypeDoc configuration, theme files, and public assets; successful rebuilds reload connected browsers automatically.
