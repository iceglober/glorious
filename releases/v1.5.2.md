# v1.5.2

Released: 2026-04-03

## Changes

### Fixed
- `install.sh` no longer requires `gh` CLI or GitHub authentication for public repos — uses the GitHub REST API via `curl` directly, matching the zero-auth install experience of tools like Claude Code
- `gh` CLI is retained as an automatic fallback for private repos or rate-limited environments
