# @glrs-dev/glorious-ask-user

The `ask_user` tool and the question widget it opens, loaded through the public
glorious extension API. It registers only where there is somebody to answer —
in print mode it withholds the tool rather than asking a question nobody can
read.
