# compaction-artifacts

When a conversation is compacted, the messages the brief replaced are written
beside the session, unchanged. This extension gives the agent tools to list,
read, annotate and delete them, so a later turn can recover the exact detail a
brief paraphrased.

See `docs/published/9-reference/5-sessions.md`.
